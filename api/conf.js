const path              = require('path');

const ANGULAR_FOLDER    = path.join(__dirname, '../app/dist/tiles/browser');
const PORT              = 3000;

module.exports = {
    ANGULAR_FOLDER,
    PORT
}