# Tiles
SD + WEBDEV Project.

#### Setup and Run

API:

- Define DB_CONNECTION_STRING (mongoDB), ANGULAR_FOLDER and PORT in `conf/.env`.
- Then run:

```bash
// Setup (Angular App)
cd app
npm i
ng build

cd ..

// Setup (Express API)
cd api
npm i

// Run
npm start
```

