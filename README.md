# Tiles
SD + WEBDEV Project.

#### Setup and Run

- Define MONGO_CONNECTION_STRING, ANGULAR_FOLDER, JWT_SECRET and PORT in `conf/.env`.
- Then execute:

```bash
// Setup (Angular App)
cd app
npm i
ng build

cd ..

// Setup (Express API)
cd api
npm i

// Run
npm start
```

